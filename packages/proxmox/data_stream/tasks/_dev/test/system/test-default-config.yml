input: httpjson
service: proxmox
vars:
  hostname: http://{{Hostname}}:{{Port}}
  user: root@pam
  token_id: xxxxxxxxxx
  secret: xxxxxxxxxx
  enable_request_tracer: true
data_stream:
  vars:
    preserve_original_event: true
